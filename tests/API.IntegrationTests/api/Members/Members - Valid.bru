meta {
  name: Members - Valid
  type: http
  seq: 1
}

get {
  url: http://localhost:5201/api/members
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("Shoould return members.", function(){
    expect(res.getStatus()).to.equal(200);
  })
  
  test("Shoould return 10 members.", function(){
    
    const jsonData = res.getBody();
    const firstMember = jsonData[0];
    
    expect(firstMember).to.have.property("id");
    expect(firstMember).to.have.property("displayName");
    
    expect(firstMember.id).to.equal("arenita-id");
    expect(firstMember.displayName).to.equal("Arenita");
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
