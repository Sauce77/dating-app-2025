meta {
  name: Login - Unauthorized Bad Email
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "jfkennedy@test.com",
    "password": ""
  }
}

tests {
  test("Should be able to login", function(){
    expect(res.getStatus()).to.equal(401);
  })
  
  
  const data = res.body;
  bru.setEnvVar("authToken", data.Token);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
