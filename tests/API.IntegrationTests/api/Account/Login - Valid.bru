meta {
  name: Login - Valid
  type: http
  seq: 1
}

post {
  url: http://localhost:5201/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "arenita@test.com",
    "password": "password"
  }
}

tests {
  test("Should be able to login", function(){
    expect(res.getStatus()).to.equal(200);
  })
  
  
  const data = res.body;
  bru.setEnvVar("authToken", data.Token);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
